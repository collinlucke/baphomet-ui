{
  "name": "baphomet-ui",
  "build": {
    "outputDirectory": "dist",
    "commands": [
      "pnpm install",
      "pnpm run build"
    ]
  },
  "routing": {
    "rules": [
      {
        "source": "/**",
        "destination": "/index.html",
        "type": "rewrite"
      }
    ]
  }
}
